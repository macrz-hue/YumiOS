suggestion_id: 2
generated_at: "2026-07-04T18:45:01Z"
target: workflow/memory
title: Add automatic session-close journaling to preserve context across restarts
prompt: Create a `scripts/session-close.sh` that the system runs at exit or heartbeat idle — it concatenates the current session's significant file writes, decisions, and errors into a structured daily note in memory/YYYY-MM-DD.md, then triggers a long-term summary merge into MEMORY.md. Wire this into AGENTS.md under the "Memory" section so the agent knows to source-control both files and prune daily notes older than 30 days. This closes the continuity gap between sessions without relying on manual note-taking.
reasoning: The system already has identity files and file I/O; session-close journaling makes it self-documenting and dramatically improves cross-session memory without adding any new runtime dependencies.
