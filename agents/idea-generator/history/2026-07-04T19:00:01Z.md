suggestion_id: 3
generated_at: "2026-07-04T19:00:01Z"
target: scripts/session-close.sh
title: Implement the referenced session-close journaling script
prompt: Create `scripts/session-close.sh` — the shell script that AGENTS.md already documents as existing. It should read `.session-log.md`, diff recent file writes from the workspace, append a structured entry to `memory/YYYY-MM-DD.md`, and auto-prune daily files older than 30 days. Wire it into a cron job or heartbeat trigger so session data is persisted without manual recall.
reasoning: The workflow is already documented but unimplemented, leaving a gap between session activity and long-term memory curation.
